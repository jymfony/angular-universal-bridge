import { NgModule } from '@angular/core';
import { ServerResponse } from '../Service/ServerResponse';

@NgModule({
    providers: [
        ServerResponse,
    ],
})
export class AngularUniversalBridgeModule {
}
